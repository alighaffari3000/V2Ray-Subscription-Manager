# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
*.db
*.db-journal
*.db-shm
*.db-wal

# Rust
V2RayDAR-main/target/
# Cargo.lock IS committed on purpose: v2raydar is a binary, not a library, and
# CI builds the released binary from it. Without the lockfile every build
# re-resolves dependencies to their newest semver-compatible versions, so a
# green build today can break tomorrow with no change on our side.

# Environments & Configs
.env
.vscode/
.idea/
instance/
dist/
build/
*.egg-info/

# graphify knowledge-graph output (rebuilt locally, never committed)
graphify-out/

# Runtime data (backups, generated storage) — must not be committed
storage/

# Vendored source archive (large blob; not needed in the repo)
V2RayDAR-main.zip
